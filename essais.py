from scipy import integrate
import scipy as sp
import numpy as np
import matplotlib.pyplot as mpl
import math

"""
test = [1, 2, 3]
print(len(test))

test.append(4)
print(len(test))
print(test)

x = np.linspace(0, 5, 100)
y = math.erf(x)


mpl.plot(x, y)       # itérable des ordonnées
mpl.xlabel('distance')      # titre des abscisses
mpl.ylabel('Puissance [W]')      # titre des ordonnées
mpl.title('erf(x)') # titre du graphique
# afficher la figure
mpl.show()
"""

jo = bytearray('C\r', 'utf-8')
print(jo)